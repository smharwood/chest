[project]
name = "chess"
version = "0.1.0"
description = "Two-player chess game built with pyxel"
requires-python = ">=3.10"
dependencies = [
    "pyxel",
]

[project.scripts]
chess = "chess_game:main"
